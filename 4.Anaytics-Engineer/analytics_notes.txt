Analyses:
 - A place for sql files that you dont want to expose.
 - I generally use it for data quality reports.

dbt_project.yml:
 - tells dbt some defaults
 - you need it to run dbt commands
 - for dbt core, your profile should match the one in the '/dbt/profile.yml'
 
macros:
 - They behavelike python functions (reusable logic)
 - They help to encapsulate logic (in one place)
 
README.md:
 - Documentation of your project
 
seed:
 - A space to upload csv and flat files (to add them to dbt later)
 - Quick and dirty approach (better to fix at source)

snapshots:
 - Take the picture of the table at a moment in time
 - Useful to track the history of the columns that oevrwrites itself.

tests:
 - A place to put assertions in SQL format
 
models:
  - staging:
	- Sources 
	- Staging files are 1 to 1 copy of your data with minimal cleaning steps.
	  DataTypes or Renaming Columns
  - Intermediate:
	- Anything that is not raw nor you want to expose.
	- No guidelines, just nice for heavy duty cleaning or complex logic
	
  - marts:
	- If it is marts, it is ready for consumption.
	- Tables ready for dashboards
	- Properly modeled, clean tables.